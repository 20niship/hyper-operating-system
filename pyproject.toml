[project]
name = "mocap-so101-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "lerobot",
]

[tool.uv.sources]
lerobot = { git = "https://github.com/huggingface/lerobot.git" }
